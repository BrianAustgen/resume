import styled from 'styled-components'
import {
  space,
  width,
  color,
  borders,
  textAlign,
  boxShadow,
  borderRadius,
  display,
  zIndex,
  maxWidth,
  minHeight
} from 'styled-system'

const Box = styled.div`
  ${space}
  ${width}
  ${color}
  ${borders}
  ${textAlign}
  ${boxShadow}
  ${borderRadius}
  ${display}
  ${zIndex}
  ${maxWidth}
  ${minHeight}
`

export default Box
