import styled from 'styled-components'
import {
  space,
  width,
  color,
  borders,
  textAlign,
  boxShadow,
  borderRadius,
  display,
  zIndex,
  maxWidth
} from 'styled-system'

const Box = styled.div`
  ${space}
  ${width}
  ${color}
  ${borders}
  ${textAlign}
  ${boxShadow}
  ${borderRadius}
  ${display}
  ${zIndex}
  ${maxWidth}
`

export default Box
