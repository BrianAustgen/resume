import styled from 'styled-components'
import {
  space,
  width,
  color,
  borders,
  textAlign,
  boxShadow,
  borderRadius,
  display,
  zIndex
} from 'styled-system'

const Box = styled.div`
  ${space}
  ${width}
  ${color}
  ${borders}
  ${textAlign}
  ${boxShadow}
  ${borderRadius}
  ${display}
  ${zIndex}
`

export default Box
